* Copy nxagentd.service to /etc/systemd/system/
* "systemctl --system daemon-reload"
* "systemctl enable --now netxms-agent"
* "systemctl enable --now netxms-server"
* "systemctl enable --now netxms-reporting"
